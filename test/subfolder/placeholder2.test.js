import { expect } from 'chai';

describe('placeholder tests in a subfolder', function() {
  it('can still add', function() {
    expect(2+2).to.equal(4);
  });
  it('can STILL add', function() {
    expect(2+2+2).to.equal(6);
  });
});
